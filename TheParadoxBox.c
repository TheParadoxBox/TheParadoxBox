#include <stdio.h>

/* Wow, you're actually looking at the source code. You're either actually somebody who programs or a dingus who thought there was something wrong with the program. */

int main() {
   printf("No files have been detected in the local directory.\n");
   printf("If you would actually like some help finding bugs, consult TheParadoxBox (The person, not the software) on Github.\n");
   
   return 0;
}
